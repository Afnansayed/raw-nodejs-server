<h1>Create a server with raw Node.js: </h1>

<p>Step 1 :  npm  init -y </p>
<p>Step 2 :  npm install -D typescript ts-node @types/node </p>
<p>Step 3 :  gitingnore node_module and .env (optional)</p>
<p>Step 4 :  tsc  – init  (if installed typescript golbay otherwise  [npx  tsc –init]</p>
<p>Step 5 : correction ts config file </p>
<p>Step 6: create a .denv file in roote  </p>
<p>Step 7: create a index.ts file in cofiq folder and install dotenv difendencies  and do confiq work And comment out  common.js line </p>
<p>Step 8: Now work with server.ts  make server and listen </p>
<p>Step 9: After add a script  (like  "dev": "ts-node ./src/server.ts")</p>
<p>Step 10:  run on terminal this command npm run dev </p>
<p>Setp 11: set confiq file use port as dynamic</p>
<p>Step 12: Before build add a script  ( "build": "tsc")</p>
<p>Setp 13: ts auto same package install for live server</p>
